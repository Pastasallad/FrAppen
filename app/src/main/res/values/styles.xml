<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/SJ_lov</item>
        <item name="colorPrimaryDark">@color/SJ_granit</item>
        <item name="colorAccent">@color/SJ_gran</item>
    </style>

</resources>
